
## **Core Boilerplate Snippets**

| **Snippet Prefix** | **Purpose**                       | **Generated Code**                                               |
| ------------------ | --------------------------------- | ---------------------------------------------------------------- |
| `javaboiler`       | Java boilerplate with main method | Creates a public class with a `main` method and `Scanner` setup. |
| `javascanner`      | Scanner boilerplate               | Creates a `Scanner` object for standard input.                   |
| `javainput`        | Basic input handling              | Reads user input from console using `Scanner`.                   |
| `javainputarr`     | Array input                       | Reads an array of size `n` from user input.                      |
| `java-arr-input`   | Array input with target variable  | Reads an array of size `n` and an additional target value.       |
| `javainputmatrix`  | 2D Matrix input                   | Reads input for a 2D array (matrix).                             |
| `arrin`            | Array input shorthand             | Takes array input from the user.                                 |

---

## **Java Data Type Inputs**

| **Snippet Prefix** | **Purpose**     | **Generated Code**                           |
| ------------------ | --------------- | -------------------------------------------- |
| `intinput`         | Integer input   | Reads an integer using `Scanner.nextInt()`.  |
| `longinput`        | Long input      | Reads a long using `Scanner.nextLong()`.     |
| `doubleinput`      | Double input    | Reads a double using `Scanner.nextDouble()`. |
| `floatinput`       | Float input     | Reads a float using `Scanner.nextFloat()`.   |
| `strinput`         | String input    | Reads a string using `Scanner.nextLine()`.   |
| `charinput`        | Character input | Reads a character from a string input.       |

---

## ️ **Java Print Statements**

| **Snippet Prefix** | **Purpose**            | **Generated Code**                      |
| ------------------ | ---------------------- | --------------------------------------- |
| `println`          | Print line             | `System.out.println()` statement.       |
| `printlnq`         | Print line with quotes | `System.out.println("text");` template. |
| `print`            | Print (no newline)     | `System.out.print()` statement.         |
| `printq`           | Print with quotes      | `System.out.print("text");` template.   |

---

##  **Java Loops**

| **Snippet Prefix** | **Purpose**            | **Generated Code**                                            |
| ------------------ | ---------------------- | ------------------------------------------------------------- |
| `for`              | Standard for loop      | Creates a `for` loop with index initialization and condition. |
| `forEach`          | Enhanced for-each loop | Iterates over collections or arrays.                          |
| `while`            | While loop             | Generates a standard `while` loop structure.                  |
| `doWhile`          | Do-while loop          | Generates a `do { } while(condition);` block.                 |

---


##  **Conditional and Switch Statements**

| **Snippet Prefix** | **Purpose** | **Generated Code** |
|---------------------|-------------|--------------------|
| `if` | If statement | Creates a simple `if` condition block. |
| `ifelse` | If-else statement | Creates an `if-else` conditional structure. |
| `ifelseif` | If-else-if statement | Creates an `if`, `else if`, and `else` chain. |
| `switch` | Switch statement | Creates a `switch` block with `case` and `default` options. |

---

##  **Java Methods**

| **Snippet Prefix** | **Purpose**              | **Generated Code**                    |
| ------------------ | ------------------------ | ------------------------------------- |
| `voidmethod`       | Void method              | Generates a `void` method template.   |
| `intmethod`        | Integer-returning method | Creates a method returning `int`.     |
| `doublemethod`     | Double-returning method  | Creates a method returning `double`.  |
| `booleanmethod`    | Boolean-returning method | Creates a method returning `boolean`. |
| `stringmethod`     | String-returning method  | Creates a method returning `String`.  |
| `charmethod`       | Char-returning method    | Creates a method returning `char`.    |
| `longmethod`       | Long-returning method    | Creates a method returning `long`.    |